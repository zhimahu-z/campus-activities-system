# 校园活动管理系统 - 后端接入完整方案

## 📊 项目现状

当前项目是**纯前端Vue3项目**，所有数据存储在浏览器的 `localStorage` 中。

### 现有技术栈
- **前端框架**: Vue 3 + Vite
- **状态管理**: Pinia
- **UI组件**: Element Plus
- **路由**: Vue Router
- **HTTP客户端**: Axios（已配置但未使用）
- **AI服务**: DeepSeek API（唯一的外部API）

### 数据存储位置
```javascript
// 用户数据
localStorage.setItem('allUsers', JSON.stringify(users))
localStorage.setItem(`favorites_${userId}`, JSON.stringify(favorites))
localStorage.setItem(`registrations_${userId}`, JSON.stringify(registrations))

// 活动数据
存储在 Pinia Store 中（内存，刷新后重新初始化）
```

---

## 🎯 三种后端接入方案

### 方案一：轻量级 - JSON Server（最快，适合演示）

**优点**：
- ✅ 5分钟快速搭建
- ✅ 零代码，自动生成RESTful API
- ✅ 适合原型演示和毕业设计答辩

**缺点**：
- ❌ 功能简单，无法实现复杂业务逻辑
- ❌ 不适合生产环境

#### 实施步骤

**1. 安装 JSON Server**
```bash
npm install -g json-server
```

**2. 创建数据文件 `db.json`**
```json
{
  "users": [
    {
      "id": 1,
      "username": "admin",
      "password": "123456",
      "name": "系统管理员",
      "email": "admin@campus.edu",
      "role": "admin",
      "avatar": "https://api.dicebear.com/7.x/avataaars/svg?seed=admin",
      "createdAt": "2026-01-01T00:00:00.000Z"
    }
  ],
  "activities": [
    {
      "id": 1,
      "title": "人工智能前沿讲座",
      "description": "邀请知名AI专家分享最新研究成果",
      "category": "学术讲座",
      "cover": "https://picsum.photos/800/400?random=1",
      "location": "学术报告厅",
      "startTime": "2026-02-15T14:00:00",
      "endTime": "2026-02-15T16:00:00",
      "maxParticipants": 200,
      "participants": 0,
      "organizer": "计算机学院",
      "tags": ["人工智能", "学术", "前沿"],
      "status": "active",
      "createdAt": "2026-01-20T10:00:00"
    }
  ],
  "registrations": [
    {
      "id": 1,
      "userId": 2,
      "activityId": 1,
      "createdAt": "2026-01-25T10:00:00"
    }
  ],
  "favorites": [
    {
      "id": 1,
      "userId": 2,
      "activityId": 1,
      "createdAt": "2026-01-25T09:00:00"
    }
  ],
  "comments": [
    {
      "id": 1,
      "activityId": 1,
      "userId": 2,
      "userName": "张三",
      "userAvatar": "https://api.dicebear.com/7.x/avataaars/svg?seed=zhang",
      "content": "这个活动很有意思！",
      "createdAt": "2026-01-25T11:00:00"
    }
  ]
}
```

**3. 启动服务器**
```bash
json-server --watch db.json --port 3000
```

**4. 修改前端代码**

创建 `src/api/index.js`：
```javascript
import request from '@/utils/request'

const API_BASE = 'http://localhost:3000'

export const userAPI = {
  // 登录
  login: (credentials) => request.post(`${API_BASE}/users?username=${credentials.username}&password=${credentials.password}`),
  
  // 获取所有用户
  getAll: () => request.get(`${API_BASE}/users`),
  
  // 创建用户
  create: (userData) => request.post(`${API_BASE}/users`, userData),
  
  // 删除用户
  delete: (id) => request.delete(`${API_BASE}/users/${id}`)
}

export const activityAPI = {
  // 获取所有活动
  getAll: () => request.get(`${API_BASE}/activities`),
  
  // 获取活动详情
  getById: (id) => request.get(`${API_BASE}/activities/${id}`),
  
  // 创建活动
  create: (data) => request.post(`${API_BASE}/activities`, data),
  
  // 更新活动
  update: (id, data) => request.put(`${API_BASE}/activities/${id}`, data),
  
  // 删除活动
  delete: (id) => request.delete(`${API_BASE}/activities/${id}`)
}

export const registrationAPI = {
  // 获取用户报名记录
  getByUser: (userId) => request.get(`${API_BASE}/registrations?userId=${userId}`),
  
  // 报名活动
  create: (data) => request.post(`${API_BASE}/registrations`, data),
  
  // 取消报名
  delete: (id) => request.delete(`${API_BASE}/registrations/${id}`)
}
```

---

### 方案二：标准方案 - Node.js + Express + MySQL（推荐）

**优点**：
- ✅ 完整的后端功能
- ✅ 数据持久化存储
- ✅ 支持复杂业务逻辑
- ✅ 适合实际部署

**缺点**：
- ❌ 需要一定开发时间（1-2天）
- ❌ 需要配置数据库

#### 项目结构

```
campus-activities-backend/
├── src/
│   ├── config/
│   │   ├── database.js      # 数据库配置
│   │   └── config.js        # 应用配置
│   ├── models/
│   │   ├── User.js          # 用户模型
│   │   ├── Activity.js      # 活动模型
│   │   ├── Registration.js  # 报名模型
│   │   └── Comment.js       # 评论模型
│   ├── controllers/
│   │   ├── userController.js
│   │   ├── activityController.js
│   │   ├── registrationController.js
│   │   └── commentController.js
│   ├── routes/
│   │   ├── user.js
│   │   ├── activity.js
│   │   ├── registration.js
│   │   └── comment.js
│   ├── middleware/
│   │   ├── auth.js          # 认证中间件
│   │   └── errorHandler.js  # 错误处理
│   └── app.js               # 应用入口
├── package.json
└── README.md
```

#### 快速搭建脚本

**1. 创建后端项目**
```bash
mkdir campus-activities-backend
cd campus-activities-backend
npm init -y
```

**2. 安装依赖**
```bash
npm install express mysql2 sequelize cors dotenv bcryptjs jsonwebtoken
npm install nodemon --save-dev
```

**3. 创建 `src/app.js`**
```javascript
const express = require('express')
const cors = require('cors')
const app = express()

// 中间件
app.use(cors())
app.use(express.json())

// 路由
app.use('/api/users', require('./routes/user'))
app.use('/api/activities', require('./routes/activity'))
app.use('/api/registrations', require('./routes/registration'))
app.use('/api/comments', require('./routes/comment'))

// 错误处理
app.use((err, req, res, next) => {
  console.error(err.stack)
  res.status(500).json({ message: '服务器错误', error: err.message })
})

const PORT = process.env.PORT || 3000
app.listen(PORT, () => {
  console.log(`🚀 服务器运行在 http://localhost:${PORT}`)
})

module.exports = app
```

**4. 数据库配置 `src/config/database.js`**
```javascript
const { Sequelize } = require('sequelize')

const sequelize = new Sequelize({
  dialect: 'mysql',
  host: 'localhost',
  port: 3306,
  username: 'root',
  password: 'your_password',
  database: 'campus_activities',
  logging: false,
  timezone: '+08:00'
})

// 测试连接
sequelize.authenticate()
  .then(() => console.log('✅ 数据库连接成功'))
  .catch(err => console.error('❌ 数据库连接失败:', err))

module.exports = sequelize
```

**5. 用户模型 `src/models/User.js`**
```javascript
const { DataTypes } = require('sequelize')
const sequelize = require('../config/database')
const bcrypt = require('bcryptjs')

const User = sequelize.define('User', {
  id: {
    type: DataTypes.INTEGER,
    primaryKey: true,
    autoIncrement: true
  },
  username: {
    type: DataTypes.STRING(50),
    unique: true,
    allowNull: false
  },
  password: {
    type: DataTypes.STRING(255),
    allowNull: false
  },
  name: {
    type: DataTypes.STRING(50),
    allowNull: false
  },
  email: {
    type: DataTypes.STRING(100),
    unique: true,
    allowNull: false
  },
  role: {
    type: DataTypes.ENUM('admin', 'student'),
    defaultValue: 'student'
  },
  avatar: {
    type: DataTypes.STRING(255)
  },
  studentId: {
    type: DataTypes.STRING(20)
  },
  major: {
    type: DataTypes.STRING(50)
  },
  grade: {
    type: DataTypes.STRING(20)
  },
  phone: {
    type: DataTypes.STRING(20)
  }
}, {
  tableName: 'users',
  timestamps: true,
  hooks: {
    beforeCreate: async (user) => {
      if (user.password) {
        user.password = await bcrypt.hash(user.password, 10)
      }
    }
  }
})

// 验证密码
User.prototype.validatePassword = async function(password) {
  return await bcrypt.compare(password, this.password)
}

module.exports = User
```

**6. 活动模型 `src/models/Activity.js`**
```javascript
const { DataTypes } = require('sequelize')
const sequelize = require('../config/database')

const Activity = sequelize.define('Activity', {
  id: {
    type: DataTypes.INTEGER,
    primaryKey: true,
    autoIncrement: true
  },
  title: {
    type: DataTypes.STRING(200),
    allowNull: false
  },
  description: {
    type: DataTypes.TEXT,
    allowNull: false
  },
  category: {
    type: DataTypes.STRING(50),
    allowNull: false
  },
  cover: {
    type: DataTypes.STRING(255)
  },
  location: {
    type: DataTypes.STRING(100),
    allowNull: false
  },
  startTime: {
    type: DataTypes.DATE,
    allowNull: false
  },
  endTime: {
    type: DataTypes.DATE,
    allowNull: false
  },
  maxParticipants: {
    type: DataTypes.INTEGER,
    defaultValue: 100
  },
  participants: {
    type: DataTypes.INTEGER,
    defaultValue: 0
  },
  organizer: {
    type: DataTypes.STRING(100),
    allowNull: false
  },
  tags: {
    type: DataTypes.JSON,
    defaultValue: []
  },
  status: {
    type: DataTypes.ENUM('active', 'inactive', 'cancelled'),
    defaultValue: 'active'
  }
}, {
  tableName: 'activities',
  timestamps: true
})

module.exports = Activity
```

**7. 用户控制器 `src/controllers/userController.js`**
```javascript
const User = require('../models/User')
const jwt = require('jsonwebtoken')

const JWT_SECRET = process.env.JWT_SECRET || 'your-secret-key'

exports.register = async (req, res) => {
  try {
    const { username, password, name, email, studentId, major, grade, phone } = req.body
    
    // 检查用户名是否存在
    const existingUser = await User.findOne({ where: { username } })
    if (existingUser) {
      return res.status(400).json({ message: '用户名已存在' })
    }
    
    // 创建用户
    const user = await User.create({
      username,
      password,
      name,
      email,
      studentId,
      major,
      grade,
      phone,
      avatar: `https://api.dicebear.com/7.x/avataaars/svg?seed=${username}`
    })
    
    res.status(201).json({
      message: '注册成功',
      user: {
        id: user.id,
        username: user.username,
        name: user.name,
        email: user.email,
        role: user.role
      }
    })
  } catch (error) {
    res.status(500).json({ message: '注册失败', error: error.message })
  }
}

exports.login = async (req, res) => {
  try {
    const { username, password } = req.body
    
    // 查找用户
    const user = await User.findOne({ where: { username } })
    if (!user) {
      return res.status(401).json({ message: '用户名或密码错误' })
    }
    
    // 验证密码
    const isValid = await user.validatePassword(password)
    if (!isValid) {
      return res.status(401).json({ message: '用户名或密码错误' })
    }
    
    // 生成token
    const token = jwt.sign(
      { id: user.id, username: user.username, role: user.role },
      JWT_SECRET,
      { expiresIn: '7d' }
    )
    
    res.json({
      message: '登录成功',
      token,
      user: {
        id: user.id,
        username: user.username,
        name: user.name,
        email: user.email,
        role: user.role,
        avatar: user.avatar,
        studentId: user.studentId,
        major: user.major,
        grade: user.grade,
        phone: user.phone
      }
    })
  } catch (error) {
    res.status(500).json({ message: '登录失败', error: error.message })
  }
}

exports.getAllStudents = async (req, res) => {
  try {
    const students = await User.findAll({
      where: { role: 'student' },
      attributes: { exclude: ['password'] }
    })
    res.json(students)
  } catch (error) {
    res.status(500).json({ message: '获取用户列表失败', error: error.message })
  }
}

exports.deleteUser = async (req, res) => {
  try {
    const { id } = req.params
    const user = await User.findByPk(id)
    
    if (!user) {
      return res.status(404).json({ message: '用户不存在' })
    }
    
    if (user.role === 'admin') {
      return res.status(403).json({ message: '不能删除管理员账号' })
    }
    
    await user.destroy()
    res.json({ message: '用户已删除' })
  } catch (error) {
    res.status(500).json({ message: '删除用户失败', error: error.message })
  }
}
```

**8. 用户路由 `src/routes/user.js`**
```javascript
const express = require('express')
const router = express.Router()
const userController = require('../controllers/userController')
const auth = require('../middleware/auth')

router.post('/register', userController.register)
router.post('/login', userController.login)
router.get('/students', auth, userController.getAllStudents)
router.delete('/:id', auth, userController.deleteUser)

module.exports = router
```

**9. 认证中间件 `src/middleware/auth.js`**
```javascript
const jwt = require('jsonwebtoken')

const JWT_SECRET = process.env.JWT_SECRET || 'your-secret-key'

module.exports = (req, res, next) => {
  try {
    const token = req.headers.authorization?.split(' ')[1]
    
    if (!token) {
      return res.status(401).json({ message: '未提供认证令牌' })
    }
    
    const decoded = jwt.verify(token, JWT_SECRET)
    req.user = decoded
    next()
  } catch (error) {
    res.status(401).json({ message: '无效的认证令牌' })
  }
}
```

**10. 数据库初始化脚本 `src/scripts/initDB.js`**
```javascript
const sequelize = require('../config/database')
const User = require('../models/User')
const Activity = require('../models/Activity')
const Registration = require('../models/Registration')
const Comment = require('../models/Comment')

async function initDatabase() {
  try {
    // 同步数据库
    await sequelize.sync({ force: true })
    console.log('✅ 数据库表创建成功')
    
    // 创建管理员账号
    await User.create({
      username: 'admin',
      password: '123456',
      name: '系统管理员',
      email: 'admin@campus.edu',
      role: 'admin',
      avatar: 'https://api.dicebear.com/7.x/avataaars/svg?seed=admin'
    })
    console.log('✅ 管理员账号创建成功')
    
    console.log('🎉 数据库初始化完成！')
    process.exit(0)
  } catch (error) {
    console.error('❌ 数据库初始化失败:', error)
    process.exit(1)
  }
}

initDatabase()
```

**11. package.json 脚本**
```json
{
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "init-db": "node src/scripts/initDB.js"
  }
}
```

#### 前端对接

**修改 `src/utils/request.js`**
```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'

const request = axios.create({
  baseURL: 'http://localhost:3000/api',  // 后端API地址
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
request.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => {
    console.error('请求错误:', error)
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  response => {
    return response.data
  },
  error => {
    if (error.response) {
      switch (error.response.status) {
        case 401:
          ElMessage.error('未授权，请重新登录')
          localStorage.removeItem('token')
          window.location.href = '/login'
          break
        case 403:
          ElMessage.error('拒绝访问')
          break
        case 404:
          ElMessage.error('请求资源不存在')
          break
        case 500:
          ElMessage.error('服务器错误')
          break
        default:
          ElMessage.error(error.response.data.message || '请求失败')
      }
    } else {
      ElMessage.error('网络错误，请检查网络连接')
    }
    return Promise.reject(error)
  }
)

export default request
```

**修改 `src/stores/user.js` - 使用API**
```javascript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import request from '@/utils/request'

export const useUserStore = defineStore('user', () => {
  const userInfo = ref(null)
  const token = ref(localStorage.getItem('token') || '')
  
  const isLoggedIn = computed(() => !!token.value)
  const isAdmin = computed(() => userInfo.value?.role === 'admin')
  
  const login = async (credentials) => {
    try {
      const response = await request.post('/users/login', credentials)
      
      token.value = response.token
      userInfo.value = response.user
      localStorage.setItem('token', response.token)
      localStorage.setItem('userInfo', JSON.stringify(response.user))
      
      return { success: true, user: response.user }
    } catch (error) {
      return { success: false, message: error.response?.data?.message || '登录失败' }
    }
  }
  
  const register = async (userData) => {
    try {
      const response = await request.post('/users/register', userData)
      return { success: true, user: response.user }
    } catch (error) {
      return { success: false, message: error.response?.data?.message || '注册失败' }
    }
  }
  
  const getAllStudents = async () => {
    try {
      const students = await request.get('/users/students')
      return students
    } catch (error) {
      console.error('获取学生列表失败:', error)
      return []
    }
  }
  
  // ... 其他方法
  
  return {
    userInfo, token, isLoggedIn, isAdmin,
    login, register, getAllStudents
  }
})
```

---

### 方案三：云服务方案 - Supabase/Firebase（最简单）

**优点**：
- ✅ 无需自己搭建服务器
- ✅ 自动处理数据库、认证、存储
- ✅ 免费额度足够小项目使用
- ✅ 提供实时数据同步

**缺点**：
- ❌ 依赖第三方服务
- ❌ 免费版有限制

#### 使用 Supabase

**1. 注册 Supabase**
访问 https://supabase.com/ 注册账号

**2. 创建项目**
创建新项目，获取 API URL 和 API Key

**3. 安装 Supabase 客户端**
```bash
npm install @supabase/supabase-js
```

**4. 创建 `src/utils/supabase.js`**
```javascript
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseKey = import.meta.env.VITE_SUPABASE_KEY

export const supabase = createClient(supabaseUrl, supabaseKey)
```

**5. 修改 Store 使用 Supabase**
```javascript
import { supabase } from '@/utils/supabase'

export const useUserStore = defineStore('user', () => {
  const login = async (credentials) => {
    const { data, error } = await supabase.auth.signInWithPassword({
      email: credentials.email,
      password: credentials.password
    })
    
    if (error) {
      return { success: false, message: error.message }
    }
    
    return { success: true, user: data.user }
  }
  
  // ... 其他方法
})
```

---

## 📋 方案对比

| 特性 | JSON Server | Node.js + MySQL | Supabase |
|------|------------|----------------|----------|
| 搭建难度 | ⭐ 极简单 | ⭐⭐⭐ 中等 | ⭐⭐ 简单 |
| 开发时间 | 5分钟 | 1-2天 | 1-2小时 |
| 功能完整性 | ⭐⭐ 基础 | ⭐⭐⭐⭐⭐ 完整 | ⭐⭐⭐⭐ 丰富 |
| 数据持久化 | ✅ | ✅ | ✅ |
| 业务逻辑 | ❌ | ✅ | ⚠️ 有限 |
| 部署难度 | ⭐ 简单 | ⭐⭐⭐ 中等 | ⭐ 简单 |
| 成本 | 免费 | 需服务器 | 免费额度 |
| 适用场景 | 演示/原型 | 生产环境 | 中小项目 |

---

## 🎯 推荐方案

### 如果是毕业设计答辩（1-2天内）
**推荐：方案一（JSON Server）**
- 快速搭建，立即可用
- 足够应对答辩演示
- 后续可以升级到完整后端

### 如果要实际部署使用
**推荐：方案二（Node.js + MySQL）**
- 完整的后端功能
- 可控性强，易于扩展
- 符合企业级开发标准

### 如果想快速上线且不想管理服务器
**推荐：方案三（Supabase）**
- 开箱即用
- 自动扩展
- 专注前端开发

---

## 🚀 快速开始（推荐方案一）

### 1分钟快速体验

```bash
# 1. 安装 JSON Server
npm install -g json-server

# 2. 在项目根目录创建 db.json（见上文）

# 3. 启动后端服务
json-server --watch db.json --port 3000

# 4. 新开终端，启动前端
npm run dev
```

现在你就有了一个带后端的完整系统！

---

## 📞 需要帮助？

如果您选择了某个方案，我可以帮您：
1. ✅ 生成完整的后端代码
2. ✅ 修改前端对接后端API
3. ✅ 配置数据库和环境
4. ✅ 部署到服务器

请告诉我您选择哪个方案，我会提供详细的实施指导！




